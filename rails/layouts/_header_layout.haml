%header#header
  .centered
    %h1
      = yield :appname

    = yield :logo
    #loading
    #nav-container
      = yield :extra_header
      %nav#appnav
        = yield :tabs
      = yield :custom_tabs
    #widget-container
      = yield :widgets
